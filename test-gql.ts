import { graphql } from 'graphql';
import { schema } from './src/graphql/schema'; // Wait, schema is constructed where?
// It's easier to just use the token from the frontend, or create a mock token.
